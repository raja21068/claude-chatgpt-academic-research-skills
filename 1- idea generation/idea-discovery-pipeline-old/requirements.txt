# ── Core (always required) ────────────────────────────────
anthropic>=0.39.0
requests>=2.31.0
PyYAML>=6.0

# ── PDF extraction (install at least one) ─────────────────
PyMuPDF>=1.23.0
# pdfplumber>=0.10.0        # fallback if PyMuPDF fails

# ── Embeddings & clustering (optional — skip with --skip-embeddings) ──
sentence-transformers>=2.2.0
numpy>=1.24.0
scikit-learn>=1.3.0
hdbscan>=0.8.33
umap-learn>=0.5.4

# ── Knowledge graph ───────────────────────────────────────
networkx>=3.1

# ── Multi-model verification (optional — set keys in .env) ─
# openai>=1.12.0            # uncomment if using GPT-4
# google-generativeai>=0.3  # uncomment if using Gemini
